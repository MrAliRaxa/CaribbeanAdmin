<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:bind="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="shopLocation"
            type="com.e.caribbeanadmin.data_model.ShopLocation" />
    </data>
    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".fragments.shop_management_component.BankLocationEdit">

        <include layout="@layout/fragment_add_locations"
            android:id="@+id/editAtmLocationLayout"
            bind:shopLocation="@{shopLocation}"
            />

    </FrameLayout>
</layout>

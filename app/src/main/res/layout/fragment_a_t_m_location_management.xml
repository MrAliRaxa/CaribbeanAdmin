<?xml version="1.0" encoding="utf-8"?>
<layout>
    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".fragments.shop_management_component.ATMLocationManagement"
        android:background="@color/white"
        android:focusable="true"
        >

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/atmLocationManagement_recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@id/addAtmLocationManagement_addBtn"
            />
        <Button
            android:id="@+id/addAtmLocationManagement_addBtn"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_alignParentBottom="true"
            android:text="Add Location"
            />

        <TextView
            android:id="@+id/addAtmLocationManagement_msg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No Location Found"
            android:layout_centerInParent="true"
            android:visibility="gone"
            />

    </RelativeLayout>
</layout>
